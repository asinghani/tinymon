name: sleep-5
results_dir_remote: "{tmpdir}/results/"
entry_cmd: echo p1; sleep 1; echo p2; sleep 1; echo p3; sleep 1; echo p4; sleep 1; echo p5; sleep 1; mkdir {tmpdir}/results; echo "success out" >> {tmpdir}/results/out.txt; echo done;
